// @flow

import type { Player } from '../../../src/scripts/types/index';
import Component from '../../../src/scripts/Components/Component';

class LoadProgress extends Component{
    constructor(player: Player, options: any = {}){

    }

    createEl(tagName?: string = "div", properties?: any, attributes?: any){
        return super.createEl("div", null, {
            className: 'vjs-panorama-load-progress'
        })
    }
}

export default LoadProgress;